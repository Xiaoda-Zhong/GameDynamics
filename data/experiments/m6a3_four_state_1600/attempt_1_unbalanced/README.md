Aborted precheck attempt. The pinned trainer's shuffle buffer of four sampled repeated items among its first four precomputation inputs, leaving state C absent. The first eight optimizer-step IDs were A, B, A, D, A, B, A, D. Training was interrupted before checkpoint 200. This partial attempt is preserved for audit and excluded from M6A-3 evaluation. The corrected run uses a one-item shuffle buffer to precompute A, B, C, D exactly once and cycle them evenly.
